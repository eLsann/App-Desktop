# =============================================================================
# ABSENSI DESKTOP - KONFIGURASI ENVIRONMENT
# =============================================================================
# Copy file ini ke .env dan sesuaikan nilai-nilainya

# -----------------------------------------------------------------------------
# KONFIGURASI DOCKER API SERVER
# -----------------------------------------------------------------------------
# Base URL API Docker (sesuaikan dengan port Docker)
API_BASE=http://localhost:8000
# API_BASE=http://localhost:8080  # Jika Docker di port 8080
# API_BASE=http://192.168.1.100:8000  # Jika Docker di remote server

# Device ID untuk identifikasi unik (wajib)
DEVICE_ID=stb-01

# Device token untuk autentikasi (wajib, dapat dari admin server)
DEVICE_TOKEN=ISI_TOKEN_DEVICE_KAMU

# -----------------------------------------------------------------------------
# KONFIGURASI KAMERA
# -----------------------------------------------------------------------------
# Index kamera (0 untuk kamera default, 1 untuk kamera kedua, dst)
CAM_INDEX=0

# Interval request ke server dalam detik (minimal 0.5, maksimal 10.0)
# Untuk Docker, sedikit lebih tinggi untuk latency
REQUEST_INTERVAL=1.5

# -----------------------------------------------------------------------------
# KONFIGURASI TEXT-TO-SPEECH (TTS)
# -----------------------------------------------------------------------------
# Voice untuk TTS (pilihan: id-ID-GadisNeural, id-ID-ArdiNeural)
EDGE_VOICE=id-ID-GadisNeural
# EDGE_VOICE=id-ID-ArdiNeural

# -----------------------------------------------------------------------------
# KONFIGURASI LOGGING
# -----------------------------------------------------------------------------
# Level logging (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# KONFIGURASI PERFORMA (DOCKER OPTIMIZED)
# -----------------------------------------------------------------------------
# Jumlah maksimal frame per detik (30 = normal, 60 = smooth)
MAX_FPS=30

# Timeout koneksi API dalam detik (lebih tinggi untuk Docker)
API_TIMEOUT=15

# -----------------------------------------------------------------------------
# KONFIGURASI KEAMANAN
# -----------------------------------------------------------------------------
# Maksimal ukuran file gambar untuk enrollment (dalam MB)
MAX_IMAGE_SIZE_MB=5

# Maksimal jumlah file untuk enrollment
MAX_IMAGE_COUNT=10

# -----------------------------------------------------------------------------
# DOCKER SPECIFIC SETTINGS
# -----------------------------------------------------------------------------
# Enable Docker-specific optimizations
DOCKER_MODE=true

# Retry attempts for Docker API
API_RETRY_ATTEMPTS=3

# Delay between retries (seconds)
API_RETRY_DELAY=1.0

# -----------------------------------------------------------------------------
# TIPS PENGATURAN DOCKER
# -----------------------------------------------------------------------------
# 1. Pastikan Docker container berjalan: docker ps
# 2. Check port mapping: docker port <container_name>
# 3. Test API health: curl http://localhost:8000/health
# 4. Test API docs: http://localhost:8000/docs
# 5. Untuk latency tinggi, naikkan REQUEST_INTERVAL ke 2.0-3.0
# 6. Untuk network issues, naikkan API_TIMEOUT ke 20-30
# 7. Gunakan test script: python test_docker_api.py
